#!/bin/sh
set -e

case "$1" in
  remove|purge)
    for alias in /usr/bin/SparkStore /usr/bin/SPARKSTORE /usr/bin/spark-store-tui; do
      if [ -L "$alias" ] && [ "$(readlink "$alias")" = "sparkstore" ]; then
        rm -f "$alias"
      fi
    done
    ;;
esac
